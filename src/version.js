/**
 * Versão do app. Atualizar manualmente a cada release significativo.
 * Mostrada no rodapé do perfil do usuário pra fim de suporte/feedback —
 * o usuário lê o número quando reporta problema, a gente sabe qual build.
 *
 * Padrão semver leve: MAJOR.MINOR
 *   - MAJOR sobe quando muda fluxo grande (ex: vira multi-empresa)
 *   - MINOR sobe a cada release com features novas
 */
export const APP_VERSION = '1.0';
